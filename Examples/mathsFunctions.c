#include <stdio.h>
#include <math.h>

int main() {
    double A = sqrt(9);
    double B = pow(2,4);
    int C = round(3.14);
    int D = ceil(3.14); //rounds up
    int E = floor(3.99); //rounds down
    double F = fabs(-100); //finds absolute value
    double G = log(3);
    double H = sin(45);
    double I = cos(45);
    double J = tan(45);
    return 0;
}